## Express rest api with PostreSQL and Sequelize

To run the run server locally, uncomment `init()` function, and run `npm run dev`.

Make to sure to install PostreSql database and create a DB `ecommerce`.
