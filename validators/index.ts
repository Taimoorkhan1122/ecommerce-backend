import { loginValidators } from "./loginValidator";
import { registerValidators } from "./registerValidator";
import { userValidator } from "./userValidator";

export  {
    loginValidators,
    registerValidators,
    userValidator
}